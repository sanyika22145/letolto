<Window x:Class="DevGamingAutoInstaller.Views.SummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Összegzés" Height="420" Width="620"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1A1F2B" Foreground="#E6ECFF">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 12">
            <TextBlock Text="Telepítés összegzés" FontSize="18" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Margin="16 0 0 0">
                <TextBlock Text="Sikeres: " Foreground="#9BE7A4"/>
                <TextBlock Text="{Binding SuccessCount}" Foreground="#9BE7A4"/>
                <TextBlock Text="  Hibás: " Margin="12 0 0 0" Foreground="#F59F9F"/>
                <TextBlock Text="{Binding FailedCount}" Foreground="#F59F9F"/>
                <TextBlock Text="  Kihagyva: " Margin="12 0 0 0" Foreground="#FFD18C"/>
                <TextBlock Text="{Binding SkippedCount}" Foreground="#FFD18C"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="#111621" CornerRadius="12" Padding="12">
            <ListView ItemsSource="{Binding Results}" Background="Transparent" BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0 4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="160"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Status}" HorizontalAlignment="Right"/>
                            <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Message}" Foreground="#8C98B3" FontSize="11"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Button Grid.Row="2" Content="Bezárás" Width="120" Height="34"
                HorizontalAlignment="Right" Margin="0 12 0 0" Click="OnClose"/>
    </Grid>
</Window>
